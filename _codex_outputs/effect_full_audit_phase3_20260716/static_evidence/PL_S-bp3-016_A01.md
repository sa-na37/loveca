# Static Evidence PL!S-bp3-016#A01

- audit_id: PL!S-bp3-016#A01
- cardnumber: PL!S-bp3-016
- cardname: 国木田花丸
- effect_text: <BODY> / 自分の成功ライブカード置き場にあるカード1枚につき、ステージにいるこのメンバーのコストを+1する。
- previous_classification: NOT_IMPLEMENTED_CONFIRMED

## cardnumber_search

- not found

## cardname_search

- not found

## feature_term_search

- `llocg_ui/engine.py:61:    {"id": "retrieve_live_then_energy_activate_if_success_score_gte", "pattern": r"^自分の控え室から『(?P<group>[^』]+)』のライブカードを(?P<n>\d+)枚手札に加える。自分の成功ライブカード置き場にあるカードのスコアの合計が(?P<score>\d+)以上の場合、エネルギーを(?P<energy_n>\d+)枚アクティブにする。$", "op": "retrieve_live_then_energy_activate_if_success_score_gte"},`
- `llocg_ui/engine.py:271:    {"id": "success_count_gain_blades_until_end_live", "pattern": r"^自分の成功ライブカード置き場にあるカード1枚につき、(?P<blades>(?:<\(ブレード\)>)+)を得る。$", "op": "success_count_gain_blades_until_end_live"},`
- `llocg_ui/engine.py:272:    {"id": "success_count_gain_blades_until_end_live_prefix", "pattern": r"^ライブ終了時まで、自分の成功ライブカード置き場にあるカード1枚につき、(?P<blades>(?:<\(ブレード\)>)+)を得る。$", "op": "success_count_gain_blades_until_end_live"},`
- `llocg_ui/engine.py:275:    {"id": "success_count_reduce_required_any", "pattern": r"^自分の成功ライブカード置き場にあるカード1枚につき、このカードを成功させるための必要ハートは(?P<anys>(?:<\(任意\)>)+)少なくなる。$", "op": "success_count_reduce_required_any"},`
- `llocg_ui/engine.py:468:    {"id": "body_always_energy_gte_cost", "pattern": r"^自分のエネルギーが(?P<n>\d+)枚以上ある場合、ステージにいるこのメンバーのコストを\+(?P<cost_n>\d+)する。$", "op": "body_always_noop"},`
- `llocg_ui/engine.py:470:    {"id": "body_always_success_score_gte_member_cost", "pattern": r"^自分の成功ライブカード置き場にあるカードのスコアの合計が(?P<n>\d+)以上である限り、ステージにいるこのメンバーのコストを\+(?P<cost_n>\d+)する。$", "op": "body_always_noop"},`
- `llocg_ui/engine.py:12767:        if 'このメンバーのコストを+' not in b and 'ステージにいるこのメンバーのコストを+' not in b:`
- `llocg_ui/engine.py:13288:                if '自分のエネルギーが' in blob and 'ステージにいるこのメンバーのコストを+' in blob:`

## compiled_db_entry

```json
{
  "ability_type": "常時",
  "trigger": "BODY",
  "conditions": "",
  "clauses": [
    {
      "optional": false,
      "cost_template": "",
      "effect_template": "自分の成功ライブカード置き場にあるカード1枚につき、ステージにいるこのメンバーのコストを+1する。",
      "cost_op": null,
      "effect_op": null,
      "raw": "自分の成功ライブカード置き場にあるカード1枚につき、ステージにいるこのメンバーのコストを+1する。"
    }
  ]
}
```

## generic_matcher_candidate

- not found

## card_specific_route

- not found

## trigger_collection_route

- `llocg_ui/server.py:418:            #   text="デッキ上2枚から『μ's』・能力なし/常時能力ありを1枚手札へ..."`
- `llocg_ui/server.py:1819:                        # 常時BODYブレード加算（コスト13以上条件）`
- `llocg_ui/server.py:1883:        """常時ブレードボーナスを返す（UI表示専用）。"""`
- `llocg_ui/server.py:1892:        """常時のハート加算を返す（UI表示専用）。"""`
- `llocg_ui/server.py:1912:        """常時のライブ合計スコア加算を返す（UI表示専用）。"""`
- `llocg_ui/server.py:2322:                # BODY trigger with clauses: ability_typeをヘッダに使う（例：常時）`

## pending_creation_route

- not found

## resolver_route

- not found

## ui_route

- not found

## reachable_from_runtime

No previous static matcher/resolve route; no pending/dispatch evidence in mapping

## conflicting_routes

none found in this static pass

- final_classification: STATIC_ANALYSIS_INCONCLUSIVE
- confidence: low
- reason: Search evidence is insufficient to confirm either implementation or absence.
